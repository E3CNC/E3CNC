# Moonraker MCP dependencies
# Pinned for reproducible deployments — update via `pip freeze` after testing
httpx>=0.27
# mcp>=1.0.0  # Optional — MCP server; install manually if needed:
#   pip3 install mcp>=1.0.0
